/**
 * Root entry point — delegates to src/main/main.js
 * This file exists because package.json "main" must point to a root-level file
 * for Electron to locate it correctly both in dev and packaged modes.
 */
require('./src/main/main.js');
